#include <stdio.h>

int main(void) {
    int n, l, v;
    n = 3;
    l = 4;

    // n += 2;
    // n -= 2;
    // n *= 2;
    // n /= 2;
    // =, +=, -=, *=, /=

    printf("%d", n);
    return 0;
}